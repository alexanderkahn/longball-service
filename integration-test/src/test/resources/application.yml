#TODO: why isn't this getting picked up from the main properties file?
application:
  name: longball-test
  version: v1000
  buildDate: yesterday
spring:
  jpa.properties.hibernate.jdbc.time_zone: UTC
  datasource.generate-unique-name: true
  liquibase:
    change-log: classpath:db/liquibase-changelog.xml
server:
  servlet:
      contextPath: /rest
oauth:
  issuer:
    algorithm: RS256
    keystore:
      url: http://fakeurl
    claims:
      aud: longball-test
      iss: https://longball.test
---
spring:
  profiles: bypassToken
oauth:
  test:
    bypassToken: nutterbutters