#TODO: why isn't this getting picked up from the main properties file?
info:
  app:
    name: @project.artifactId@
    version: @project.version@
    buildDate: @buildDate@
spring:
  jpa.properties.hibernate.jdbc.time_zone: UTC
  datasource.generate-unique-name: true
  liquibase:
    change-log: classpath:db/liquibase-changelog.xml
server:
  servlet:
      contextPath: /rest
oauth:
  issuer:
    algorithm: RS256
    keystore:
      url: http://fakeurl
    claims:
      audience: longball-test
      issuer: https://longball.test
  unauthenticated-paths:
    - /actuator/info
    - /actuator/health
    - /swagger-ui.*
    - /webjars/springfox-swagger-ui/.*
    - /swagger-resources.*
    - /v2/api-docs
---
spring:
  profiles: bypassToken
oauth:
    bypass-token:
      token: nutterbutters