#TODO: why isn't this getting picked up from the main properties file?
server:
  contextPath: /rest
application:
  name: longball-test
  version: v1000
  buildDate: yesterday
liquibase:
  change-log: classpath:db/liquibase-changelog.xml
oauth:
  issuer:
    algorithm: RS256
    keystore:
      url: http://fakeurl
    claims:
      aud: longball-test
      iss: https://longball.test
---
spring:
  profiles: test
  jpa:
    #TODO: this doesn't work yet -- needs Hibernate 5.2.3. See about upgrading.
    #For now run tests with -Duser.timezone=UTC command line argument
    properties.hibernate.jdbc.time_zone: UTC
    hibernate.ddl-auto: none
  datasource.generate-unique-name: true
---
spring:
  profiles: bypassToken
oauth:
  test:
    bypassToken: nutterbutters