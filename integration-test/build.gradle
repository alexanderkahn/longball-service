description = ''

configurations {
    testCompile.extendsFrom(restAssured)
    testCompile.extendsFrom(springBootStarterTest)
    testRuntime.extendsFrom(h2)
}

dependencies {
    testCompile project(':longball-core')
}
