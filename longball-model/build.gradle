
description = ''
configurations {

    compile.extendsFrom(jsonApiModel)
    compile.extendsFrom(commonsLang3)

    testCompile.extendsFrom(hibernateValidator)
    testCompile.extendsFrom(javaxEl)
}
